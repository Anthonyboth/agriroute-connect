workflows:
  ios-release-testflight:
    name: iOS Release Build (IPA Only)
    max_build_duration: 60
    environment:
      xcode: latest
      node: 20.x
      groups:
        - app_store_credentials
      vars:
        BUNDLE_ID: "app.lovable.f2dbc20153194f90a3cc8dd215bbebba"
    scripts:
      - name: Install dependencies
        script: |
          npm ci
      
      - name: Audit Capacitor versions
        script: |
          bash ./audit-deps.sh
      
      - name: Build web app
        script: |
          npm run build
      
      - name: Sync Capacitor
        script: |
          npx cap sync ios
      
      - name: Fetch signing files
        script: |
          app-store-connect fetch-signing-files \
            $BUNDLE_ID \
            --type IOS_APP_STORE \
            --create
      
      - name: Add certificates to keychain
        script: |
          keychain add-certificates
      
      - name: Set up code signing settings
        script: |
          xcode-project use-profiles
      
      - name: Increment build number
        script: |
          cd ios/App
          agvtool new-version -all $(($BUILD_NUMBER + 1))
      
      - name: Build IPA for App Store
        script: |
          xcode-project build-ipa \
            --workspace ios/App/App.xcworkspace \
            --scheme App \
            --archive-flags="-destination 'generic/platform=iOS'" \
            --export-options-plist ios/App/exportOptions.plist
    
    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
    
    # REMOVED: publishing block with auth: integration
    # Reason: requires workflow-level "integrations: app_store_connect" config
    # which was not defined. Re-add when integration is configured in Codemagic UI.
    # See: https://docs.codemagic.io/yaml-publishing/app-store-connect/
    
    publishing:
      email:
        recipients:
          - dev@agriroute.com
        notify:
          success: true
          failure: true
